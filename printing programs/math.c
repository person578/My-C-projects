#include <stdio.h>
#include <math.h>

int main() 
{
    int x = 3;

    // x = sqrt(x);
    // x = pow(x, 4);
    // x = round(x);
    // x = ceil(x);
    x = sqrt(x);

    printf("%d\n", x);

    return 0;
}