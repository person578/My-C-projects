#include <stdio.h>
#include <math.h>

int main() 
{
    float math = 9;

    // x = sqrt(x);
    // x = pow(x, 4);
    // x = round(x);
    // x = ceil(x);
    math = sqrt(math);

    printf("%f\n", math);

    return 0;
}