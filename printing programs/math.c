#include <stdio.h>
#include <math.h>

int main() 
{
    float x = 3;

    // x = sqrt(x);
    // x = pow(x, 4);
    // x = round(x);
    // x = ceil(x);
    // math = sqrt(math);
    x = log(x);

    printf("%f\n", x);

    return 0;
}